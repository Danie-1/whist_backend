-r requirements.txt
black==23.1.0              # must match version in .pre-commit-config.yaml
isort==5.12.0              # must match version in .pre-commit-config.yaml
flake8==6.0.0              # must match version in .pre-commit-config.yaml
flake8-bugbear==23.3.12	   # must match version in .pre-commit-config.yaml
flake8-noqa==1.3.1         # must match version in .pre-commit-config.yaml
tox==4.4.7
pytest==7.2.2
pytest-cov==4.0.0
mypy==1.1.1
